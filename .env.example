# FastAPI RAG chat base URL (no trailing slash).
# Leave empty to use local knowledge fallback in the browser.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Contact form destination
CONTACT_TO_EMAIL=ismanovoybek75@gmail.com

# Optional: Gmail SMTP (recommended for reliable delivery)
# 1) Google Account → Security → 2-Step Verification ON
# 2) App passwords → create one for Mail
# 3) Paste the 16-character password below
GMAIL_USER=ismanovoybek75@gmail.com
GMAIL_APP_PASSWORD=
